{% extends 'basecnx.html.twig' %}

{% block title %}Annuler la Réservation - RecyConnect{% endblock %}

{% block body %}
    <!-- Page Header Start -->
    <div class="container-fluid page-header py-5 mb-5 wow fadeIn" data-wow-delay="0.1s">
        <div class="container text-center py-5">
            <h1 class="display-3 text-white mb-4 animated slideInDown">Annuler la Réservation</h1>
            <nav aria-label="breadcrumb animated slideInDown">
                <ol class="breadcrumb justify-content-center mb-0">
                    <li class="breadcrumb-item"><a href="{{ path('events_list') }}" class="breadcrumb-link">Accueil</a></li>
                    <li class="breadcrumb-item"><a href="{{ path('reservations_list') }}" class="breadcrumb-link">Mes Réservations</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Annuler la Réservation</li>
                </ol>
            </nav>
        </div>
    </div>
    <!-- Page Header End -->

    <!-- Cancel Reservation Section Start -->
    <section id="cancel-reservation" class="cancel-reservation section py-5">
            <div class="row justify-content-center">
                <div class="col-lg-8 wow fadeIn" data-wow-delay="0.1s">
                    <div class="bg-light rounded shadow-lg p-4">
                        <h2 class="mb-4 text-center fw-bold display-4 text-primary">
                            Confirmer l'Annulation
                        </h2>
                        <div class="reservation-details mb-4">
                            <p class="fs-4"><strong>🎟 Événement:</strong> {{ reservation.eventId.nomEvent }}</p>
                            <p class="fs-4"><strong>👤 Nom du participant:</strong> {{ reservation.nom }}</p>
                            <p class="fs-4"><strong>📌 Nombre de places réservées:</strong> {{ reservation.nbPlaces }}</p>
                            <p class="fs-4"><strong>⚠️ Statut:</strong> {{ reservation.status }}</p>
                        </div>

                        <form action="{{ path('reservation_cancel', {'id': reservation.id}) }}" method="POST">
                            <input type="hidden" name="_method" value="POST">
                            <input type="hidden" name="_token" value="{{ csrf_token('cancel' ~ reservation.id) }}">

                            <div class="d-flex justify-content-between gap-3 mt-4">
                                <button type="submit" class="btn btn-success btn-lg py-3 px-5 fw-bold">
                                    <i class="fas fa-check me-2"></i> Oui, annuler la réservation
                                </button>
                                <a href="{{ path('reservations_list') }}" class="btn btn-secondary btn-lg py-3 px-5 fw-bold">
                                    <i class="fas fa-arrow-left me-2"></i> Non, retour à la liste
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
    </section>
    <!-- Cancel Reservation Section End -->
{% endblock %}
