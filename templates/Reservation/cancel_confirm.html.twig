{% extends 'basecnx.html.twig' %}

{% block title %}Annuler la Réservation{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Confirmer l'Annulation</h1>

        <div class="card p-4 shadow-sm">
            <h4>Êtes-vous sûr de vouloir annuler cette réservation ?</h4>
            <p><strong>Événement:</strong> {{ reservation.getEventId().getNomEvent()}}</p>
            <p><strong>Nom du participant:</strong> {{ reservation.getNom() }}</p>
            <p><strong>Nombre de places réservées:</strong> {{ reservation.getNbPlaces() }}</p>
            <p><strong>Statut:</strong> {{ reservation.getStatus() }}</p>

            <form action="{{ path('reservation_cancel', {'id': reservation.id}) }}" method="POST" class="mt-4">
                <input type="hidden" name="_method" value="POST">
                <input type="hidden" name="_token" value="{{ csrf_token('cancel' ~ reservation.id) }}">

                <div class="text-center">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-times"></i> Oui, annuler la réservation
                    </button>
                    <a href="{{ path('reservations_list') }}" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Non, retour à la liste
                    </a>
                </div>
            </form>
        </div>
    </div>
{% endblock %}