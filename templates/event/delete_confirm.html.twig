{% extends 'base2.html.twig' %}

{% block title %}Confirmer la Suppression{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-body">
                <h1 class="card-title text-center mb-4">Confirmer la Suppression</h1>

                <div class="alert alert-warning" role="alert">
                    <h4 class="alert-heading">Êtes-vous sûr de vouloir supprimer cet événement ?</h4>
                    <p class="mb-0">Cette action est irréversible. Toutes les données associées à cet événement seront supprimées.</p>
                </div>

                <div class="mb-4">
                    <p><strong>Événement:</strong> {{ event.nomEvent }}</p>
                    <p><strong>Date:</strong> {{ event.dateEvent|date('d/m/Y') }}</p>
                    <p><strong>Lieu:</strong> {{ event.lieuEvent }}</p>
                </div>

                <form action="{{ path('delete_event', {'id': event.id}) }}" method="POST">
                    <input type="hidden" name="_method" value="POST">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">

                    <div class="text-center d-flex justify-content-center gap-3">
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-trash"></i> Oui, supprimer
                        </button>
                        <a href="{{ path('admin_events') }}" class="btn btn-secondary">
                            <i class="fas fa-times"></i> Non, annuler
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}